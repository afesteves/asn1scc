version: 3.3.20.{build}

build:
  parallel: true
  project: Asn1.sln
  verbosity: normal

before_build:
  - DOSKEY make="c:\cygwin64\bin\make.exe"
  - DOSKEY python3="c:\Python36-x64\python.exe"
  - cd Tests
  - make
